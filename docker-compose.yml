services:
  agenticbotplatform:
    build: .
    ports:
      - "8787:8787"
    volumes:
      - ./.env:/app/.env
      - agenticbotplatform-data:/app/data
      - agenticbotplatform-config:/app/config
    restart: unless-stopped

volumes:
  agenticbotplatform-data:
  agenticbotplatform-config:
